import * as styles from "./Radio.css";

const Radio = () => {
  return (
    <input
      type="radio"
      className={styles.radio}
      // value={value}
      // checked={isChecked}
      // onChange={() => onChange(value)}
    />
  );
};

export default Radio;
